# RustyLLM GitHub Pages

This is a standalone static site with no build step. The existing
`.github/workflows/pages.yml` workflow publishes this folder at the GitHub Pages
root and keeps the WASM demo available at `/demo/`.

The language choice is saved locally in the browser and otherwise follows the
browser language.
